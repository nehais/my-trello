export interface taskActivity {
  taskActId: number;
  name: string;
  desc: string;
  date: Date;
  new: boolean;
}

export interface newTaskActivity {
  taskActId: number;
  name: string;
  desc: string;
  date: Date;
  new: boolean;
}
